drupalstat.ajaxToggleMaintenanceMode:
    path: '/drupalstat/toggle-maintenance-mode'
    defaults:
        _controller: '\Drupal\drupalstat\Controller\DrupalStatController::toggleMaintenanceMode'
        _title: 'Toggle Maintenance Mode'
    requirements:
        _permission: 'access administration pages'

drupalstat.ajaxRebuildCache:
    path: '/drupalstat/rebuild-drupal-cache'
    defaults:
        _controller: '\Drupal\drupalstat\Controller\DrupalStatController::rebuildDrupalCache'
        _title: 'Rebuild Drupal Cache'
    requirements:
        _permission: 'access administration pages'

drupalstat.ajaxCleanupOldFiles:
    path: '/drupalstat/cleanup-old-files'
    defaults:
        _controller: '\Drupal\drupalstat\Controller\DrupalStatController::cleanupOldFiles'
        _title: 'Cleanup Old Files'
    requirements:
        _permission: 'access administration pages'

drupalstat.purgeSessions:
    path: '/drupalstat/purge-sessions'
    defaults:
        _controller: '\Drupal\drupalstat\Controller\DrupalStatController::purgeSessions'
        _title: 'Purge Sessions'
    requirements:
        _permission: 'access administration pages'
